import React from "react";
export interface ScreenHeightComponentProps {
    children: React.ReactNode;
}
export declare function ScreenHeightComponent({ children, }: ScreenHeightComponentProps): React.JSX.Element;
